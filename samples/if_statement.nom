//! 50

fn main() -> i32 {
    var res: i32 = 0;
    val a: bool = false;
    val b: bool = true;

    if (a) {
        res = res + 10;
    }; // Semicolon required (for now)

    if b { // The parentheses around the condition are optional (and discouraged). The block is required though.
        res = res + 20;
    };

    if true {
        res = res + 30;
    };

    res
}
