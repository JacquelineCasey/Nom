//! 39

fn g(a: i32, b: i32) -> i32 {
    a + b
}

fn f(a: i32, b: i32, c: i32, d: i32) -> i32 {
    val x: i32 = g(a, 2 * b);
    val y: i32 = g(3 * c, 4 * d);

    x + y
}

fn main() -> i32 {
    var a: i32 = 1;
    val b: i32 = 10;
    f(a, a + 1, b, a)
}
