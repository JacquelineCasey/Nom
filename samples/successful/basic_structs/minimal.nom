//! 105

struct int_pair {
    first: i32,
    second: i32,
}

fn make_pair(a: i32, b: i32) -> int_pair {
    int_pair { first: a, second: b }
}

fn main() -> i32 {
    val pair: int_pair = make_pair(3, 5);

    pair.first = 100;

    pair.first + pair.second
}