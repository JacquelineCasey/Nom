//! 30

fn main() -> i32 {
    var a: i32 = 0;

    a += 10;
    a -= 5;
    a *= 6;
    a /= 2;

    // a = 15

    var count: i32 = 0;
    while count < 15 {
        a += 1;
        count += 1;
    };

    a
}
