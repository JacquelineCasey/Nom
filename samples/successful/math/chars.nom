//! 10

fn main() -> i32 {
    val a: u8 = 44;
    var b: u8 = 32;
    b = a + b + b;
    // b = b * 4;  // uncomment this: it overflows!

    var c: u8 = 255;
    // c = c + 1;  // overflows!
    
    10  // Main must be i32. No promotion yet.
}
